spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/link_shortcut
    username: postgres
    password: ovedet92
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  application:
    name: shortCut
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    encoding: UTF-8
